{
  "name": "xrails-complier",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/xrails",
    "build": "tsc",
    "prepublish": "typings install && npm run build"
  },
  "devDependencies": {
    "typescript": "next"
  },
  "dependencies": {
    "doctrine": "^1.2.1",
    "nunjucks": "^2.4.2"
  }
}
