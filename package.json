{
  "name": "xrails-complier",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/xrails",
    "build": "tsc",
    "prepublish": "typings install && npm run build"
  },
  "devDependencies": {
    "typescript": "next"
  },
  "dependencies": {
    "doctrine": "^1.2.1",
    "esprima": "^2.7.2",
    "winston": "^2.2.0"
  }
}
